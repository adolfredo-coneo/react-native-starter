import React from "react";
import { Text, StyleSheet } from "react-native";

const HomeScreen = () => {
  return <Text style={styles.text}>HomeScreen Adol</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 40,
    textAlign: "center"
  }
});

export default HomeScreen;
